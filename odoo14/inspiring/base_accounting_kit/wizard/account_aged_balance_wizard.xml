<odoo>
    <record id="account_aged_balance_wizard_new" model="ir.ui.view"> 
        <field name="name">Aged Partner Balance</field>
        <field name="model">account.aged.trial.balance.new</field>  
        <field name="arch" type="xml">
            <form string="Report Options"> 
                <group col="4">
                    <field name="date_from"/>
                    <field name="period_length"/>
                    <field name="partner_id" invisible="1" />
                    <newline/>
                    <!-- <field name="result_selection" widget="radio"/>
                    <field name="target_move" widget="radio"/> -->
                </group>
                <field name="journal_ids" required="0" invisible="1"/>
                <footer>
                    <button name="print_account_aged_balance" string="Print" type="object" default_focus="1" class="oe_highlight"/>
                    <button string="Cancel" class="btn btn-default" special="cancel"/> 
                </footer>
            </form>
        </field>
    </record>
</odoo>